.DS_Store
node_modules
/build
/.svelte-kit
/package
/dist
.env
.env.*
.dev.vars
.dev.vars.*
!.dev.vars.example
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.wrangler
wrangler.toml.local

# LLM outputs (test logs, coverage dumps, etc.)
.llm-outputs/
*.log

# Agent session memory. Untracked but previously not ignored, so a `git add -A`
# could publish transcript scratch. vite.config.ts already excludes it from
# coverage; this keeps it out of commits too.
.remember/

# Testing
coverage/
.nyc_output/
test-results/
playwright-report/
playwright/.cache/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
